[package]
name = "camelid-enterprise-engine-windows"
description = "Windows engine backend: host capability detection and the x86-64 AVX2 Q8_0 kernel."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "engine_windows"

[dependencies]
engine-core.workspace = true
