apiVersion: v1
kind: Service
metadata:
  name: camelid-enterprise-deterministic
  labels:
    app: camelid-enterprise
    lane: deterministic
spec:
  selector:
    app: camelid-enterprise
    lane: deterministic
  ports:
    - name: http
      port: 80
      targetPort: 8181
